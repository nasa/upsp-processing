/** @file
 *  @brief  Test the projection.h functions
 *  @date   May 9, 2019
 *  @author jmpowel2
 */

#ifndef TEST_PROJECTION_H_
#define TEST_PROJECTION_H_

#include "../include/projection.h"

/* TODO - still need tests for:
 *
 * contains(cv::Size, cv::Point2i)
 * OccludedPredicate
 * ObliquePredicate
 * ProjectionPredicate
 * GreaterXPredicate
 * VisiblePredicate
 * NonDataNodePredicate
 * make_combined_predicate
 * make_visible_predicate
 * intersects(BoundingBox, Ray)
 * intersect_time(BoundingBox, Ray)
 * intersect_time(Polyhedron, Ray)
 * intersects(Triangle, Ray)
 * simple_ray_trace
 * ray_trace
 * find_nodes(Octree, Polyhedron)
 * create_projection_mat
 * identify_skipped_nodes (2)
 * project_frame
 * adjust_projection_for_weights
 * get_target_diameters
 * write_projection_matrix
 * read_projection_matrix
 */

#endif /* TEST_PROJECTION_H_ */
